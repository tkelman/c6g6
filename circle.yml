machine:
  services:
    - docker

test:
  override:
    - docker build -t tkelman/c6g6 .

deployment:
  hub:
    commands:
      - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
      - docker push tkelman/c6g6
